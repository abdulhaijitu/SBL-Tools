# SBL Marketing — Render.com ডেপ্লয়মেন্ট গাইড (পূর্ণাঙ্গ ও সহজ নির্দেশিকা)

এই প্রজেক্টটি এখন **[Render.com](https://render.com)**-এ নেটিভ ডকার কনটেইনারের মাধ্যমে লাইভ করার জন্য সম্পূর্ণ প্রস্তুত। এখানে Cloudflare-এর মতো কোনো স্ট্যাটিক প্রাক-রেন্ডারিং বা জাভাস্ক্রিপ্ট সিমুলেশন নেই—আসল Laravel ফ্রেমওয়ার্ক রিয়েল-টাইম চলবে, ফলে **লিড ডিলিট, মেম্বার প্লেসমেন্ট, টাস্ক ট্র্যাকিং সবকিছু ১০০% নির্ভুলভাবে কাজ করবে**।

---

## পদ্ধতি ১: Blueprint দিয়ে ১-ক্লিক ডিপ্লয় (সবচেয়ে সহজ)

১. [Render.com](https://dashboard.render.com/)-এ লগইন করুন।
২. ড্যাশবোর্ডের উপরে **"New +"** বাটনে ক্লিক করে **"Blueprint"** সিলেক্ট করুন।
৩. আপনার GitHub রিপোজিটরি (`abdulhaijitu/SBL-Tools`) সিলেক্ট করুন।
৪. Render স্বয়ংক্রিয়ভাবে আমাদের তৈরি করা `render.yaml` ডিটেক্ট করবে।
৫. **"Apply"** বাটনে ক্লিক করুন! Render নিজে থেকেই Docker ইমেজ তৈরি করবে, ডাটাবেজ মাইগ্রেশন ও সিডিং চালাবে এবং সাইট লাইভ করে দেবে।

---

## পদ্ধতি ২: ম্যানুয়াল Web Service তৈরি

যদি আপনি নিজে ম্যানুয়ালি Web Service তৈরি করতে চান:

১. Render ড্যাশবোর্ডে **"New +"** > **"Web Service"** ক্লিক করুন।
২. আপনার গিটহাব রিপোজিটরি কানেক্ট করুন।
৩. নিচের সেটিংসগুলো দিন:

- **Name**: `sbl-marketing`
- **Region**: `Singapore` (বাংলাদেশ থেকে সবচেয়ে দ্রুত গতির জন্য)
- **Branch**: `main`
- **Runtime**: **`Docker`**
- **Instance Type**: **`Free`**
  ৪. **Environment Variables** সেকশনে নিচের ভ্যারিয়েবলগুলো অ্যাড করুন:
  | Key | Value |
  | :--- | :--- |
  | `APP_NAME` | `SBL Marketing` |
  | `APP_ENV` | `production` |
  | `APP_DEBUG` | `false` |
  | `APP_KEY` | _(Generate বাটনে ক্লিক করুন অথবা লোকালহোস্টের `.env` থেকে কপি করুন)_ |
  | `DB_CONNECTION` | `sqlite` |
  | `DB_DATABASE` | `/var/www/html/database/database.sqlite` |
  | `LOG_CHANNEL` | `stderr` |
  | `SESSION_DRIVER` | `database` |

৫. **"Create Web Service"**-এ ক্লিক করুন।

---

## ৩. ডাটাবেজ সংক্রান্ত তথ্য

- **SQLite (ডিফল্ট)**: কনটেইনার প্রথমবার চালু হওয়ার সময় স্বয়ংক্রিয়ভাবে `database.sqlite` ফাইল তৈরি হবে, মাইগ্রেশন চলবে এবং অ্যাডমিন অ্যাকাউন্ট সিড হয়ে যাবে।
- **Render Managed PostgreSQL (ঐচ্ছিক - সেরা স্থায়িত্ব)**:
    - Render-এ একটি Free PostgreSQL ডাটাবেজ তৈরি করে তার `Internal Database URL` কপি করে আপনার Web Service-এর `DATABASE_URL` বা `DB_URL`-এ পেস্ট করে দিলেই স্বয়ংক্রিয়ভাবে পোস্টগ্রেস ডাটাবেজে রূপান্তর হয়ে যাবে!

---

## ৪. লাইভ হলে লগইন করবেন যেভাবে

- **ইউআরএল**: `https://sbl-marketing.onrender.com` (বা আপনার সার্ভিসের Render লিংক)
- **অ্যাডমিন ইমেইল**: `admin@sbl.test`
- **পাসওয়ার্ড**: `password`

---

## ৫. লিড ডিলিট টেস্ট

লগইন করে Leads মেনু থেকে যেকোনো লিড ডিলিট বাটনে ক্লিক করুন। সাথে সাথে লিডটি ডাটাবেজ থেকে মুছে যাবে এবং পেজ রিলোড বা রিফ্রেশ করলেও এটি আর ফিরে আসবে না!
